# 更新日志

> [English](CHANGELOG.en.md)

## 0.4.4 - 2026-08-05

- 新增月海、松烟、朱砂、雾蓝四套预定义主题；主题切换、预览、持久化和每日更新均使用同一套内置配色。

## 0.4.3 - 2026-08-05

- 新增上边距与下边距调节；四个方向的版式滑杆都会实时刷新预览，并在“应用到桌面”后供每日更新使用。

## 0.4.2 - 2026-08-05

- 颜色选择器在调色过程中实时刷新设置窗口预览；取消会恢复原状态，确认后仍需点击“应用到桌面”才会保存设置和更新壁纸。
- 新增可拖动的左侧安全区与右侧边距，预览随参数即时变化；边距会在“应用到桌面”后持久化并供每日更新使用。

## 0.4.1 - 2026-08-05

- 修复原生窗口丢失 `wParam` 的消息转发：右上角关闭、最小化、最大化和系统光标恢复正常。
- 自定义颜色确认后立即保存主题并更新桌面，不再需要额外点击“应用到桌面”。

## 0.4.0 - 2026-08-05

- 将设置体验重写为 Win32 原生窗口，不再打开浏览器或本地网页。
- 原生窗口使用与实际壁纸相同的 Rust 渲染器，提供全年预览、深浅主题、完整配色编辑、导入、导出和应用操作。
- 移除旧版 HTML、JavaScript、CSS 和 `yuexu://` 网页协议；升级时自动清理协议注册。
- 日常更新改为按当前主屏的物理分辨率生成壁纸，减少无效渲染并避免系统二次缩放或裁切。

## 0.3.0 - 2026-08-05

- 支持导出、导入和持久化自定义主题 JSON；颜色输入限制为安全的 `#RRGGBB`。
- 预览页新增自定义主题导入颜色样本，导入后立即由原生程序生成壁纸。
- 安装和开发脚本默认保留已有主题，避免升级覆盖用户自定义配色。

## 0.2.0 - 2026-08-05

- 重写为 Rust 原生运行时，移除 Go 及 Chromium 截图渲染路径。
- 使用 `lunar_rust` 在本地计算农历、闰月和干支；不再依赖浏览器 ICU 数据。
- 由 Rust 生成 SVG，并在进程内通过 `resvg` / `tiny-skia` 直接输出 PNG。
- 壁纸更新不再启动 Chrome、Edge 或 WebView；只有手动预览仍会按需打开本地设置页。
- 调整全年网格布局：保留左侧桌面图标安全区，并保留右侧边距。

## 0.1.0 - 2026-08-04

- 首次发布：全年 12 个月农历桌面日历。
- 深色、浅色两套主题，默认深色并记住用户选择。
- 基于 Chromium 内置中国农历数据，支持闰月与 1900-2100 年。
- 原生 Windows 启动器：静态壁纸生成后不保留浏览器进程。
- 用户级安装、桌面/开始菜单入口、登录和每日自动更新。
- 版本化 Windows x64 发行包、SHA256 校验和与卸载项。
